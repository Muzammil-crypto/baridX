enum TransitionType { fade, slide, scale }
enum PaymentMethod { creditCard, cashOnDelivery, payLater, goBack }
enum OrderStatus { idle, loading, success, failure }
enum AppLayoutType { standard, noBack }
